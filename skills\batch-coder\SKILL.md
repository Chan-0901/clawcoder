---
name: batch-coder
description: 批量代码操作工具箱。用于：(1) 批量替换文件中的文本 (2) 批量重命名文件 (3) 批量从模板生成文件 (4) 批量查找包含特定内容的文件 (5) 批量修改文件扩展名。触发场景："把所有文件里的XXX改成YYY"、"批量生成100个文件"、"批量重命名这个文件夹"、"查找所有包含XXX的文件"
---

# Batch Coder

批量代码操作的瑞士军刀。让小墨能够高效处理大量文件的重复工作。

## 核心能力

### 1. 批量文本替换
```
.\batch-replace.ps1 -Path <目录> -Find <查找内容> -Replace <替换内容> [-Pattern <文件模式>] [-Recurse]
```
- 支持正则表达式
- 支持指定文件类型（如 *.py, *.js）
- 支持递归子目录
- **执行前先预览**

### 2. 批量文件重命名
```
.\batch-rename.ps1 -Path <目录> -Pattern <模式> -Replace <替换> [-Files <文件列表>]
```
- 支持通配符
- 支持编号序列（如 file_{1:3}.txt → file_001.txt, file_002.txt, file_003.txt）

### 3. 批量模板生成
```
.\batch-generate.ps1 -Template <模板文件> -Data <数据文件> -Output <输出目录>
```
- 从CSV/JSON数据读取变量
- 模板使用 `{{变量名}}` 占位符
- 支持嵌套循环生成

### 4. 批量文件搜索
```
.\batch-grep.ps1 -Path <目录> -Pattern <搜索内容> [-Files <文件类型>] [-Recurse] [-ShowContext <行数>]
```

### 5. 批量扩展名修改
```
.\batch-ext.ps1 -Path <目录> -From <原扩展名> -To <新扩展名>
```

## 使用流程

### 批量替换（最常用）

1. **明确范围**：告诉我要处理哪个目录、什么类型的文件
2. **确认模式**：告诉我查找和替换的内容
3. **预览**：我会先显示将要修改的内容
4. **确认执行**：你说"执行"我才真正修改

### 模板生成

1. **提供模板**：给我一个包含 `{{变量名}}` 的模板文件
2. **提供数据**：给我CSV或JSON格式的数据
3. **指定输出**：告诉我输出到哪里

## 示例

### 示例1：批量替换变量名
```
用户：把这个文件夹里所有JS文件的 userId 都改成 uid
小墨：执行 batch-replace.ps1 -Path . -Find 'userId' -Replace 'uid' -Pattern '*.js' -Recurse
```

### 示例2：批量生成配置文件
```
用户：用这个模板生成10个配置文件，每个的DB_NAME分别是 db1 到 db10
小墨：
1. 模板: config.txt 含 {{DB_NAME}}
2. 数据: CSV格式
3. 执行批量生成
```

### 示例3：查找包含关键词的文件
```
用户：在这个项目里找出所有使用了 'TODO' 注释的文件
小墨：执行 batch-grep.ps1 -Path . -Pattern 'TODO' -Files '*.py' -Recurse -ShowContext 2
```

## 安全机制

- 所有修改操作默认是**预览模式**
- 需要用户明确确认后才执行
- 执行前自动备份原文件（到 `.backup/` 目录）
- 记录所有操作到操作日志

## 触发条件

- 用户说"批量"、"所有文件"、"批量生成"
- 用户说"把所有XXX改成YYY"
- 用户说"这个文件夹里有哪些文件包含"
