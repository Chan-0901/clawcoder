---
name: code-reviewer
description: 代码审查与质量分析工具。用于：(1) 全面审查代码找出潜在问题 (2) 检测安全漏洞（SQL注入、XSS等） (3) 识别性能问题 (4) 检查代码规范 (5) 提出改进建议。触发场景："帮我review代码"、"检查这段代码有没有问题"、"看看有没有安全漏洞"、"代码规范检查"
---

# Code Reviewer

深度代码审查专家。能发现肉眼容易忽略的问题。

## 审查维度

### 1. 🔒 安全漏洞检测
```
高危:
- SQL注入: 直接拼接SQL字符串
- XSS: 未转义的用户输入输出
- 认证绕过: 硬编码密码/密钥
- 敏感信息泄露: console.log敏感数据

中危:
- 文件操作: 路径遍历
- 命令注入: system/exec直接执行
- CSRF: 缺少token验证
```

### 2. ⚡ 性能问题
```
- N+1查询问题
- 内存泄漏: 全局变量累积
- 同步阻塞: 大文件同步读取
- 重复计算: 循环内重复调用
```

### 3. 📝 代码规范
```
- 命名不规范
- 魔法数字/字符串
- 过长函数(>100行)
- 深层嵌套(>4层)
- 缺少错误处理
```

### 4. 🐛 逻辑错误
```
- 空指针引用
- 边界条件未处理
- 并发问题
- 状态不一致
```

## 使用流程

### 快速审查
```
用户: 帮我审查这段代码
小墨: 执行 code-review.ps1 -Content "代码" -Level fast
```

### 全面审查
```
用户: 深度审查这个项目
小墨: 执行 code-review.ps1 -Path <项目路径> -Level full
```

### 指定维度
```
用户: 只检查安全问题和性能
小墨: 执行 code-review.ps1 -Path . -Checks security,performance
```

## 审查报告格式

```markdown
# 📋 代码审查报告

## 📊 概述
- 文件数: X
- 总问题数: X
  - 🔴 高危: X
  - 🟡 中危: X  
  - 🟢 低危: X

## 🔴 高危问题

### 1. SQL注入风险
- 文件: `src/db.js`
- 行号: 23
- 问题代码:
```javascript
query("SELECT * FROM users WHERE id=" + userId)
```
- 建议: 使用参数化查询
```

## 🟡 中危问题
...

## 🟢 建议改进
...
```

## 触发条件

- 用户说"审查代码"、"review"
- 用户说"检查有没有漏洞"
- 用户说"看看这段代码怎么样"
- 用户要求"代码规范检查"

## 内置检查规则

### 安全规则库
| 模式 | 风险 | 建议 |
|-----|------|-----|
| `'+ str(` | SQL注入 | 参数化查询 |
| `.innerHTML =` | XSS | 文本节点替代 |
| `eval(` | 代码注入 | 避免eval |
| `password = '` | 硬编码密码 | 环境变量 |
| `exec(` | 命令注入 | 白名单 |

### 性能规则库
| 模式 | 风险 | 建议 |
|-----|------|-----|
| `for` + `query` | N+1 | 批量查询 |
| 循环内API | 性能损耗 | 提取到循环外 |
| 递归无memo | 重复计算 | 添加缓存 |
