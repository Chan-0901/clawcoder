---
name: exec-hook
description: 执行钩子系统 - 在操作执行前后注入自定义逻辑。用于：(1) 操作前后自动记录日志 (2) 文件修改前自动备份 (3) 执行结果摘要生成 (4) 操作历史追踪 (5) 自动检查危险操作。触发场景：执行任何有副作用的操作（写文件、执行命令、删除文件）时自动激活
---

# Exec Hook System

执行钩子系统 - 让小墨的每一次重要操作都可追踪、可审计、可回滚。

## 核心能力

### 1. 🔄 操作拦截
在执行危险操作前自动暂停，询问确认：
- 文件删除/移动
- 批量修改
- 系统命令执行
- 外部网络请求

### 2. 📝 操作日志
自动记录所有执行的操作到日志文件：
```
时间: 2026-04-02 22:45:12
操作: edit
文件: C:\project\main.py
变更: 替换行 23-25
备份: .backup/main.py.20260402224512
```

### 3. 💾 自动备份
- 每次修改文件前自动备份到 `.backup/` 目录
- 保留最近10个版本
- 支持一键回滚

### 4. 📊 执行摘要
操作完成后自动生成摘要：
- 修改了哪些文件
- 变更内容是什么
- 是否有错误
- 如何回滚

## 内置钩子

### before_exec
执行系统命令前：
1. 检查是否危险命令
2. 记录命令内容
3. 要求确认（如果危险）

### before_write
写入文件前：
1. 检查目标路径
2. 自动创建备份
3. 记录变更内容

### before_delete
删除文件前：
1. 确认文件重要性
2. 移动到回收站而非直接删除
3. 记录删除操作

### after_exec
执行命令后：
1. 检查退出码
2. 记录输出摘要
3. 报告异常

## 使用方法

钩子系统默认启用，无需手动调用。

当检测到危险操作时，小墨会自动：
1. 暂停操作
2. 显示将要执行的内容
3. 等待确认
4. 执行并记录

## 触发条件

**自动触发**：
- 执行 `edit`, `write`, `exec` 等工具时
- 检测到删除操作时
- 检测到批量操作时

**手动触发**：
```
用户: 帮我追踪一下我刚才做了哪些操作
小墨: 读取操作日志，显示历史
```

## 日志位置

- 主日志: `memory/exec-hooks/exec.log`
- 备份目录: `.backup/`
- 回滚记录: `memory/exec-hooks/rollback.json`

## 回滚命令

如需回滚操作：
```
用户: 回滚上一个操作
小墨: 从备份恢复，撤销变更
```
