---
name: project-indexer
description: 项目结构索引与代码库理解工具。当需要分析项目结构、理解代码调用关系、查找特定函数/类的位置、或者需要记住整个项目的布局时触发。用于：(1) 分析项目文件结构 (2) 提取函数和类定义 (3) 建立调用关系索引 (4) 搜索代码模式 (5) 生成项目文档摘要
---

# Project Indexer

项目索引器 - 让小墨能够真正"理解"项目结构，而不只是读取文件。

## 核心能力

1. **扫描项目结构** - 递归扫描目录，生成文件树
2. **提取代码要素** - 从代码文件中提取函数、类、变量定义
3. **建立索引** - 生成可持久化的索引文件
4. **语义搜索** - 在项目中搜索特定的代码模式

## 使用流程

### 第一步：索引项目

当用户说"索引这个项目"或"分析这个项目结构"时：

```
执行 scripts/index-project.ps1 <项目根目录> [输出文件]
```

### 第二步：查询索引

索引完成后，可以：
- "查找所有包含 'user' 的文件"
- "找到 Login 函数在哪里定义"
- "这个项目有哪些主要的类"

### 第三步：持久化记忆

索引结果会自动存入 `memory/project-indexes/` 目录，下次对话可以直接使用。

## 索引文件格式

索引结果保存为 Markdown 格式：

```markdown
# 项目索引: <项目名>
时间: <索引时间>

## 文件结构
<文件树>

## 代码要素
### 函数
- `functionName` - file:line - 描述

### 类
- `ClassName` - file:line - 描述

## 调用关系
- `FuncA` → `FuncB` (file:line)
```

## 触发条件

- 用户提到"分析项目"、"理解代码库"、"项目结构"
- 用户要求"查找某个函数"、"找到定义"
- 用户要求"这个项目有哪些X"

## 注意事项

- 索引是增量更新的，只重新索引改动的文件
- 大项目可以指定只索引特定目录
- 索引结果存入记忆，供后续对话使用
