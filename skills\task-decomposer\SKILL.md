---
name: task-decomposer
description: 复杂任务分解与并行执行调度器。当用户提出复杂任务、需要分解成多个子任务执行、或者需要并行处理多个独立工作项时触发。用于：(1) 将大型任务拆解为可管理的子任务 (2) 识别任务依赖关系 (3) 调度多个子任务并行执行 (4) 汇总各子任务结果 (5) 生成执行报告。触发场景："这个任务太复杂了"、"帮我分解一下"、"这件事有很多步骤"、"并行处理这些"
---

# Task Decomposer

复杂任务的分解与调度专家。让小墨能够处理那些"一句话但实际上很复杂"的任务。

## 核心概念

### 任务图
```
     复杂任务
        │
        ▼
    ┌───┴───┐
    │       │
   子任务A  子任务B  ← 可并行
    │       │
    └─┬─────┘
      │
      ▼
   子任务C  ← 依赖A和B的结果
```

### 任务类型
- **并行任务**：相互独立，可同时执行
- **顺序任务**：依赖前一个完成
- **条件任务**：根据情况选择执行

## 执行流程

### 1. 接收任务
用户说："帮我分析这个项目的问题并修复"

### 2. 分解任务
```
分解为：
├── 子任务1: 代码语法检查
├── 子任务2: 安全漏洞扫描
├── 子任务3: 性能问题分析
└── 子任务4: 生成修复报告
```

### 3. 执行调度
- **先执行**：独立的子任务用 `sessions_spawn` 并行执行
- **再执行**：依赖任务在前置任务完成后执行

### 4. 结果汇总
- 收集各子任务的执行结果
- 生成统一的执行报告
- 如有失败，指出问题所在

## 使用方法

当遇到复杂任务时，小墨会自动：

1. **分析任务结构**：识别需要做的所有事情
2. **识别依赖**：确定哪些可以并行，哪些必须顺序执行
3. **分配执行**：使用 subagent 并行处理独立任务
4. **跟踪进度**：监控各子任务状态
5. **汇总交付**：整合结果，给出完整答案

## 示例

### 示例1：代码审查任务
```
用户：帮我全面审查这个项目
分解：
1. [并行] 检查代码规范
2. [并行] 扫描安全漏洞
3. [并行] 分析性能瓶颈
4. [顺序] 生成审查报告
```

### 示例2：数据处理任务
```
用户：处理这100个Excel文件
分解：
1. [并行] 每批10个，分10批处理
2. [顺序] 汇总所有结果
3. [顺序] 生成最终报告
```

### 示例3：内容创作任务
```
用户：帮我写一个完整的小说章节
分解：
1. [并行] 构思情节发展
2. [并行] 设计对白
3. [并行] 描写场景
4. [顺序] 整合润色
```

## 触发条件

- 用户说"这个很复杂"、"帮我分解"
- 用户提出一个包含多个步骤的任务
- 用户要求"同时做这些事情"
- 小墨自己判断任务太复杂需要分解

## 调度策略

### 并行度控制
- 默认最多 3-4 个并行任务
- 根据任务复杂度动态调整
- 避免资源竞争

### 错误处理
- 单个子任务失败不影响其他任务
- 最终报告标注失败的任务
- 提供重试建议

### 进度反馈
- 告知用户当前执行的子任务
- 完成后给出简要总结
